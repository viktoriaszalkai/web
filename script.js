function betumeretValtas() {
    const body = document.body;
    body.style.fontSize = body.style.fontSize === "1.2rem" ? "1rem" : "1.2rem";
}
console.log("Oldal betöltve, sávok és kockák inicializálva.");